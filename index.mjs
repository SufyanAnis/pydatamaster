// Root entry point for hosts that expect a single start file (e.g. Hostinger Node.js Web Apps).
// Run `npm run build` first; this simply boots the compiled server, which also serves the built client.
import "./server/dist/index.js";
